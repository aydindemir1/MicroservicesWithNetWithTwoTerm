 SELECT
	PRORECID,
	SIRANO,
	GUVENCE2,
	ISNULL(PLANTURU,
	'') AS PLANTURU,
	EMEKLI,
	ACIL,
	TARIH,
	KODU,
	KURUMADI,
	ACIKLAMA,
	AVANS,
	ADET,
	FIYATI,
	TUTARI,
	KURAL,
	TABLEX,
	KASAKONT,
	TF,
	SEVKSIZ,
	POLIKLINIKADI
FROM
	(
	SELECT
		0 PRORECID,
		P.GUVENCE2,
		'PROTOKOLY' TABLEX,
		P.SIRANO,
		convert(bit,
		0) ACIL,
		33 KURAL,
		P.TARIH,
		K.KURUMADI,
		'' KODU,
		'Yatış Ücretleri (' + PO.POLIKLINIKADI + ')' ACIKLAMA,
		0 ADET,
		0.00 FIYATI,
		0.00 TUTARI,
		0 KASAKONT,
		CONVERT(BIT,
		1) TF,
		CONVERT(BIT,
		0) SEVKSIZ,
		P.ALINANAVANS * -1 AVANS,
		PO.POLIKLINIKADI,
		P.PLANTURU,
		CASE
			WHEN P.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ')
			AND P.SIGORTALITURU IN('H', 'K', 'D') THEN 'ÇALIŞAN'
			WHEN P.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ')
			AND(ISNULL(P.SIGORTALITURU,
			'E') = 'E'
			OR P.SIGORTALITURU = '') THEN 'EMEKLİ'
			ELSE ''
		END EMEKLI
	FROM
		V_PROTOKOLY P WITH(NOLOCK)
	INNER JOIN POLIKLINIK PO WITH(NOLOCK) ON
		P.PoliklinikID = PO.PoliklinikID
	INNER JOIN KURUMLAR K WITH(NOLOCK) ON
		K.KurumID = P.KurumID
	WHERE
		(P.ALINANAVANS < 0)
		AND(P.BELGEKONT = 2)
		AND(P.BARKODKODU = 21290798)
		AND ISNULL((
		SELECT
			COUNT(PP.PRORECID) AS SAYI
		FROM
			PROTOKOL40 PP WITH(NOLOCK)
		WHERE
			(PP.PROTOKOL = P.SIRANO)
				AND(PP.KODU > '00005')
					AND(PP.FIYATI > 0)),
		0) = 0 ) A
UNION ALL
SELECT
	0 PRORECID,
	P.GUVENCE2,
	'PROTOKOLY' TABLEX,
	P.SIRANO,
	convert(bit,
	0) ACIL,
	100 KURAL,
	P.TARIH,
	K.KURUMADI,
	'' KODU,
	'Yatış Ücretleri (' + PO.POLIKLINIKADI + ')' ACIKLAMA,
	0 ADET,
	0.00 FIYATI,
	0.00 TUTARI,
	0 KASAKONT,
	CONVERT(BIT,
	1) TF,
	CONVERT(BIT,
	0) SEVKSIZ,
	P.ALINANAVANS * -1 AVANS,
	PO.POLIKLINIKADI,
	P.PLANTURU,
	CASE
		WHEN P.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ')
		AND P.SIGORTALITURU IN('H', 'K', 'D') THEN 'ÇALIŞAN'
		WHEN P.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ')
		AND(ISNULL(P.SIGORTALITURU,
		'E') = 'E'
			OR P.SIGORTALITURU = '') THEN 'EMEKLİ'
		ELSE ''
	END EMEKLI
FROM
	V_PROTOKOLY P WITH(NOLOCK)
INNER JOIN POLIKLINIK PO WITH(NOLOCK) ON
	P.PoliklinikID = PO.PoliklinikID
INNER JOIN KURUMLAR K WITH(NOLOCK) ON
	K.KurumID = P.KurumID
WHERE
	(P.ALINANAVANS < 0)
	AND(P.BELGEKONT = 1)
	AND(P.BARKODKODU = 21290798)
	AND ISNULL((
	SELECT
		COUNT(PP.PRORECID) AS SAYI
	FROM
		PROTOKOL40 PP WITH(NOLOCK)
	WHERE
		(PP.PROTOKOL = P.SIRANO)
			AND(PP.KODU > '00005')
				AND(PP.FIYATI > 0)),
	0) = 0
UNION ALL
SELECT
	0 PRORECID,
	P.GUVENCE2,
	'PROTOKOL' TABLEX,
	P.SIRANO,
	P.ACIL,
	1 KURAL,
	P.TARIH,
	'' KURUMADI,
	'' KODU,
	'Muayene Farkı-Katkı Payı (' + PO.POLIKLINIKADI + ')' ACIKLAMA,
	1 ADET,
	P.FIYATI3 FIYATI,
	P.FIYATI3 TUTARI,
	KASAKONT3 KASAKONT,
	CONVERT(BIT,
	1) TF,
	P.SEVKSIZRESMI SEVKSIZ,
	0.00 AVANS,
	PO.POLIKLINIKADI,
	P.PLANTURU,
	CASE
		WHEN P.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ')
		AND P.SIGORTALITURU IN('H', 'K', 'D') THEN 'ÇALIŞAN'
		WHEN P.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ')
		AND(ISNULL(P.SIGORTALITURU,
		'E') = 'E'
			OR P.SIGORTALITURU = '') THEN 'EMEKLİ'
		ELSE ''
	END EMEKLI
FROM
	V_PROTOKOL P WITH(NOLOCK)
INNER JOIN POLIKLINIK PO WITH(NOLOCK) ON
	P.PoliklinikID = PO.PoliklinikID
WHERE
	(P.KASAKONT3 = 0)
	AND(P.FIYATI3 > 0)
	AND(P.BARKODKODU = 21290798)
UNION ALL
SELECT
	PP.PRORECID,
	P.GUVENCE2,
	'PROTOKOL20' TABLEX,
	P.SIRANO,
	P.ACIL,
	31 KURAL,
	P.TARIH,
	K.KURUMADI,
	'' KODU,
	CASE
		WHEN PP.ACIKLAMA LIKE '%SİLAH RUHSATI%' THEN 'Muayene (' + PO.POLIKLINIKADI + ')'
		ELSE 'Muayene Ücreti (' + PO.POLIKLINIKADI + ')'
	END ACIKLAMA,
	PP.ADET_CARP ADET,
	PP.FIYATI,
	PP.ADET_CARP * PP.FIYATI TUTARI,
	0 KASAKONT,
	CONVERT(BIT,
	1) TF,
	P.SEVKSIZRESMI SEVKSIZ,
	0.00 AVANS,
	PO.POLIKLINIKADI,
	P.PLANTURU,
CASE
    WHEN P.GUVENCE2 IN ('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ')
         AND P.SIGORTALITURU IN ('H', 'K', 'D') 
    THEN 'ÇALIŞAN'
    WHEN P.GUVENCE2 IN ('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ')
         AND (ISNULL(P.SIGORTALITURU, 'E') = 'E' OR P.SIGORTALITURU = '') 
    THEN 'EMEKLİ'
    ELSE ''
END AS EMEKLI
FROM
	V_PROTOKOL P WITH(NOLOCK)
INNER JOIN PROTOKOL20 PP WITH(NOLOCK)ON
	P.SIRANO = PP.PROTOKOL
INNER JOIN KURUMLAR K WITH(NOLOCK) ON
	K.KurumID = P.KurumID
INNER JOIN POLIKLINIK PO WITH(NOLOCK) ON PP.PoliklinikID = PO.PoliklinikID
WHERE
	(PP.KASAKONT = 0)
	AND(PP.FIYATI > 0)
	AND(P.BARKODKODU = 21290798)
	AND(P.PROVIZYONNO <> 'Yurt dışı ssk')
	AND(PP.ACIKLAMA LIKE '%MUAYENE%')
UNION ALL
SELECT
	0 PRORECID,
	PR.GUVENCE2,
	'PROTOKOL20' TABLEX,
	PR.SIRANO,
	PR.ACIL,
	32 KURAL,
	PR.TARIH,
	K.KURUMADI,
	'' KODU,
	'' ACIKLAMA,
	1 ADET,
	SUM(PP.ADET_CARP * PP.FIYATI) FIYATI,
	SUM(PP.ADET_CARP * PP.FIYATI)TUTARI,
	0 KASAKONT,
	CONVERT(BIT,
	1) TF,
	PR.SEVKSIZRESMI SEVKSIZ,
	0.00 AVANS,
	PO.POLIKLINIKADI,
	PR.PLANTURU,
	CASE
		WHEN PR.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ')
		AND PR.SIGORTALITURU IN('H', 'K', 'D') THEN 'ÇALIŞAN'
		WHEN PR.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ')
		AND(ISNULL(PR.SIGORTALITURU,
		'E') = 'E'
			OR PR.SIGORTALITURU = '') THEN 'EMEKLİ'
		ELSE ''
	END EMEKLI
FROM
	V_PROTOKOL PR WITH(NOLOCK)
INNER JOIN PROTOKOL20 PP WITH(NOLOCK) ON
	PR.SIRANO = PP.PROTOKOL
INNER JOIN KURUMLAR K WITH(NOLOCK) ON
	K.KurumID = PR.KurumID
INNER JOIN POLIKLINIK PO WITH(NOLOCK) ON
	PR.PoliklinikID = PO.PoliklinikID
WHERE
	(PP.KODU > '00004')
	AND(PP.KASAKONT = 0)
	AND(PP.ACIKLAMA NOT LIKE '%MUAYENE%')
	AND(PR.BARKODKODU = 21290798)
	AND(PP.FIYATI > 0)
	AND(PP.KODU NOT IN(
	SELECT
		P20.KODU
	FROM
		V_PROTOKOL P WITH(NOLOCK),
		PROTOKOL20 P20 WITH(NOLOCK),
		UCRET1 U WITH(NOLOCK) WHERE P.BARKODKODU = 21290798
			AND P20.PROTOKOL = P.SIRANO
			AND P20.KASAKONT = 0
			AND P20.KODU = U.KODU
			AND P20.FIYATI > 0
			AND(((P.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', '2022', '1005', 'VATANSIZ', 'YABANCI', 'YABANCI İSTEK', '60-G', '18 YAŞ ALTI', 'YABANCI ÖĞRENCİ', 'KAPSAM DIŞI', 'STAJYER AVUKAT', 'AİHM', 'ACİL HAL', 'TBMM', 'S.G.K.'))
				AND(P.SIGORTALITURU = 'E')
					AND(U.KATPAY_SGKE> 0))
				OR((P.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', '2022', '1005', 'VATANSIZ', 'YABANCI', 'YABANCI İSTEK', '60-G', '18 YAŞ ALTI', 'YABANCI ÖĞRENCİ', 'KAPSAM DIŞI', 'STAJYER AVUKAT', 'AİHM', 'ACİL HAL', 'TBMM', 'S.G.K.'))
					AND(P.SIGORTALITURU IS NULL
						OR P.SIGORTALITURU IN('H', 'K', 'D', ''))
						AND(U.KATPAY_SGKC > 0))
					OR((P.GUVENCE2 = '4447')
						AND(P.TARIH >= '2010-04-01'))
						OR((P.GUVENCE2 = '2828')
							AND(P.TARIH >= '2010-04-01'))
							OR((P.GUVENCE2 = 'YEŞİLKART')
								AND(P.TARIH >= '2010-04-01')
									AND(U.KATPAY_YESILKART > 0))
								OR((P.GUVENCE2 = 'YEŞİLKART')
									AND(P.TARIH < '2010-04-01')
										AND(U.KATPAY_SGKE > 0))
									OR((P.GUVENCE2 = 'RESMİ')
										AND(U.KATPAY_RESMI > 0))) ) )
GROUP BY
	PR.PLANTURU,
	PR.SIGORTALITURU,
	PR.GUVENCE2,
	PR.SEVKSIZRESMI,
	PR.BARKODKODU,
	PR.SIRANO,
	PR.ACIL,
	PR.TARIH,
	PO.POLIKLINIKADI,
	K.KURUMADI
UNION ALL
SELECT
	0 PRORECID,
	P.GUVENCE2,
	'PROTOKOL40' TABLEX,
	P.SIRANO,
	convert(bit,
	0) ACIL,
	33 KURAL,
	P.TARIH,
	K.KURUMADI,
	'' KODU,
	'Yatış Ücretleri(' + PO.POLIKLINIKADI + ')' ACIKLAMA,
	1 ADET,
	SUM(PP.ADET_CARP * PP.FIYATI) FIYATI,
	SUM(PP.ADET_CARP * PP.FIYATI) TUTARI,
	0 KASAKONT,
	CONVERT(BIT,
	1) TF,
	CONVERT(BIT,
	0) SEVKSIZ,
	P.ALINANAVANS * -1 AVANS,
	PO.POLIKLINIKADI,
	P.PLANTURU,
CASE
    WHEN P.GUVENCE2 IN ('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ')
         AND P.SIGORTALITURU IN ('H', 'K', 'D') 
    THEN 'ÇALIŞAN'
    WHEN P.GUVENCE2 IN ('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ')
         AND (ISNULL(P.SIGORTALITURU, 'E') = 'E' OR P.SIGORTALITURU = '') 
    THEN 'EMEKLİ'
    ELSE ''
END AS EMEKLI
	FROM
		V_PROTOKOLY P WITH(NOLOCK)
	INNER JOIN PROTOKOL40 PP WITH(NOLOCK) ON
		P.SIRANO = PP.PROTOKOL
	INNER JOIN KURUMLAR K WITH(NOLOCK) ON
		K.KurumID = P.KurumID
	INNER JOIN POLIKLINIK PO WITH(NOLOCK) ON
		P.PoliklinikID = PO.PoliklinikID
	WHERE
		(P.ALINANAVANS <= 0)
		AND(P.ALINANAVANS2 <= 0)
		AND(PP.KODU <> '00010')
		AND(PP.KODU <> '00001')
		AND(PP.KASAKONT = 0)
		AND(P.SIRANO IN(
		SELECT
			P.SIRANO
		FROM
			PROTOKOLY P WITH(NOLOCK)
		INNER JOIN PROTOKOL40 PP WITH(NOLOCK) ON
			P.SIRANO = PP.PROTOKOL
		INNER JOIN POLIKLINIK PO WITH(NOLOCK) ON
			P.PoliklinikID = PO.PoliklinikID
		WHERE
			(P.ALINANAVANS <= 0)
				AND(P.ALINANAVANS2 <= 0)
					AND(PP.KODU <> '00010')
						AND(PP.KODU <> '00001')
							AND(PP.KASAKONT = 0)
								AND(P.BARKODKODU = 21290798)
									AND(PP.FIYATI > 0)
										AND((
										SELECT
											COUNT(P40.PRORECID)
										FROM
											PROTOKOL40 P40 WITH(NOLOCK) WHERE P40.PROTOKOL = P.SIRANO
												AND P40.KODU IN('510010', '510020', '510030', '510040', '510050', '510060', '510070', '510080', '510090', '510090_1', '510100', '510110', '510120')) > 0)
											AND(P.BELGEKONT = 2)
										GROUP BY
											P.SIRANO))
		AND(PP.FIYATI > 0) GROUP BY P.PLANTURU,
		P.SIGORTALITURU,
		P.GUVENCE2,
		P.SIRANO,
		P.TARIH,
		PO.POLIKLINIKADI,
		P.ALINANAVANS,
		KURUMADI UNION ALL
SELECT
	0 PRORECID,
	P.GUVENCE2,
	'PROTOKOL40' TABLEX,
	P.SIRANO,
	convert(bit,
	0) ACIL,
	33 KURAL,
	P.TARIH,
	K.KURUMADI,
	'' KODU,
	'Yatış Ücretleri (' + PO.POLIKLINIKADI + ')' ACIKLAMA,
	1 ADET,
	SUM(PP.ADET_CARP * PP.FIYATI) FIYATI,
	SUM(PP.ADET_CARP * PP.FIYATI) TUTARI,
	0 KASAKONT,
	CONVERT(BIT,
	1) TF,
	CONVERT(BIT,
	0) SEVKSIZ,
	P.ALINANAVANS * -1 AVANS,
	PO.POLIKLINIKADI,
	P.PLANTURU,
	CASE
		WHEN P.GUVENCE2 IN ('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ')
		AND P.SIGORTALITURU IN('H', 'K', 'D') THEN 'ÇALIŞAN'
		WHEN P.GUVENCE2 IN ('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ')
		AND(ISNULL(P.SIGORTALITURU,
		'E') = 'E'
			OR P.SIGORTALITURU = '') THEN 'EMEKLİ'
		ELSE ''
	END EMEKLI
FROM
	V_PROTOKOLY P WITH(NOLOCK)
INNER JOIN PROTOKOL40 PP WITH(NOLOCK) ON
	P.SIRANO = PP.PROTOKOL
INNER JOIN KURUMLAR K WITH(NOLOCK) ON
	K.KurumID = P.KurumID
INNER JOIN POLIKLINIK PO WITH(NOLOCK) ON
	P.PoliklinikID = PO.PoliklinikID
WHERE
	(KODU NOT IN('510010', '510020', '510030', '510040', '510050', '510060'))
	AND(P.ALINANAVANS <= 0)
	AND(P.ALINANAVANS2 <= 0)
	AND(PP.KODU <> '00010')
	AND (PP.KODU <> '00001')
	AND(PP.KASAKONT = 0)
	AND(P.SIRANO IN(
	SELECT
		P.SIRANO
	FROM
		PROTOKOLY P WITH(NOLOCK)
	INNER JOIN PROTOKOL40 PP WITH(NOLOCK) ON
		P.SIRANO = PP.PROTOKOL
	INNER JOIN POLIKLINIK PO WITH(NOLOCK) ON P.PoliklinikID = PO.PoliklinikID
	WHERE
		(P.ALINANAVANS <= 0)
			AND(P.ALINANAVANS2 <= 0)
				AND(PP.KODU <> '00010')
					AND(PP.KODU <> '00001')
						AND(PP.KASAKONT = 0)
							AND(P.BARKODKODU = 21290798)
								AND(PP.FIYATI > 0)
									AND((
									SELECT
										COUNT (P40.PRORECID)
									FROM
										PROTOKOL40 P40 WITH(NOLOCK)
									WHERE
										P40.PROTOKOL = P.SIRANO
										AND P40.KODU IN('510010', '510020', '510030', '510040', '510050', '510060', '510070', '510080', '510090', '510090_1', '510100', '510110', '510120')) > 0)
										AND(P.BELGEKONT IS NULL
											OR P.BELGEKONT <> 2)
									GROUP BY
										P.SIRANO))
	AND(PP.FIYATI > 0)
GROUP BY
	P.PLANTURU,
	P.SIGORTALITURU,
	P.GUVENCE2,
	P.SIRANO,
	P.TARIH,
	PO.POLIKLINIKADI,
	P.ALINANAVANS,
	KURUMADI UNION ALL
SELECT
	P40.PRORECID,
	PY.GUVENCE2,
	'PROTOKOL40' TABLEX,
	P40.PROTOKOL SIRANO,
	convert(bit,
	0) ACIL,
	7 KURAL,
	P40.ISLEMTAR TARIH,
	K.KURUMADI,
	P40.KODU,
	P40.ACIKLAMA,
	P40.ADET_CARP ADET,
	P40.FIYATI,
	P40.ADET_CARP * P40.FIYATI TUTARI,
	0 KASAKONT,
	CONVERT(BIT,
	1) TF,
	CONVERT(BIT,
	0) SEVKSIZ,
	0.00 AVANS,
	PO.POLIKLINIKADI,
	PY.PLANTURU,
	CASE
		WHEN PY.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ')
		AND PY.SIGORTALITURU IN('H', 'K', 'D') THEN 'ÇALIŞAN'
		WHEN PY.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ')
		AND(ISNULL(PY.SIGORTALITURU,
		'E') = 'E'
			OR PY.SIGORTALITURU = '') THEN 'EMEKLİ'
		ELSE ''
	END EMEKLI
FROM
	PROTOKOL40 P40 WITH(NOLOCK)
INNER JOIN V_PROTOKOLY PY WITH(NOLOCK) ON
	P40.PROTOKOL = PY.SIRANO 
INNER JOIN KURUMLAR K WITH(NOLOCK) ON K.KurumID = PY.KurumID
INNER JOIN POLIKLINIK PO WITH(NOLOCK) ON  P40.PoliklinikID = PO.PoliklinikID
WHERE
	(P40.KASAKONTYF = 0)
	AND(P40.KODU = '00010')
	AND(P40.BARKODKODU = 21290798)
UNION ALL
SELECT
	0 PRORECID,
	P.GUVENCE2,
	'PROTOKOLY' TABLEX,
	P.SIRANO,
	convert(bit,
	0) ACIL,
	CASE
		WHEN P.ALINANAVANS > 0 THEN 4
		ELSE 5
	END KURAL,
	P.TARIH,
	K.KURUMADI,
	'' KODU,
	'Yataklı Nakit ' + CASE
		WHEN P.ALINANAVANS > 0 THEN ''
		ELSE 'EK '
	END + 'Avansı (' + PO.POLIKLINIKADI + ')' ACIKLAMA,
	1 ADET,
	CASE
		WHEN P.ALINANAVANS > 0 THEN P.ALINANAVANS
		ELSE P.ALINANAVANS2
	END FIYATI,
	CASE
		WHEN P.ALINANAVANS > 0 THEN P.ALINANAVANS
		ELSE P.ALINANAVANS2 END TUTARI,
		'ALINANAVANS' + CASE
			WHEN P.ALINANAVANS > 0 THEN ''
			ELSE '2'
		END KASAKONT,
		CONVERT(BIT,
		1) TF,
		CONVERT(BIT,
		0) SEVKSIZ,
		CASE
			WHEN P.ALINANAVANS > 0 THEN 0
			ELSE P.ALINANAVANS * -1
		END AVANS,
		PO.POLIKLINIKADI,
		P.PLANTURU,
		CASE
			WHEN P.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ')
			AND P.SIGORTALITURU IN ('H', 'K', 'D') THEN 'ÇALIŞAN'
			WHEN P.GUVENCE2 IN ('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ')
			AND(ISNULL(P.SIGORTALITURU,
			'E') = 'E'
				OR P.SIGORTALITURU = '') THEN 'EMEKLİ'
			ELSE ''
		END EMEKLI
	FROM
		V_PROTOKOLY P WITH(NOLOCK)
	INNER JOIN POLIKLINIK PO WITH(NOLOCK) ON
		P.PoliklinikID = PO.PoliklinikID
	INNER JOIN KURUMLAR K WITH(NOLOCK) ON
		K.KurumID = P.KurumID WHERE((P.ALINANAVANS > 0)
			Or(P.ALINANAVANS2 > 0))
		AND(P.BARKODKODU = 21290798)
		AND(P.ODEMETAMAM = 0)
UNION ALL
SELECT
	0 PRORECID,
	P.GUVENCE2,
	'PROTOKOL' TABLEX,
	P.SIRANO,
	convert(bit,
	0) ACIL,
	CASE
		WHEN P.ALINANAVANS > 0 THEN 4
		ELSE 5
	END KURAL,
	P.TARIH,
	K.KURUMADI,
	'' KODU,
	'Ayaktan Nakit ' + CASE
		WHEN P.ALINANAVANS > 0 THEN ''
		ELSE 'EK '
	END + 'Avansı (' + PO.POLIKLINIKADI + ')' ACIKLAMA,
	1 ADET,
	CASE
		WHEN P.ALINANAVANS > 0 THEN P.ALINANAVANS
		ELSE P.ALINANAVANS2
	END FIYATI,
	CASE
		WHEN P.ALINANAVANS > 0 THEN P.ALINANAVANS
		ELSE P.ALINANAVANS2
	END TUTARI,
	'ALINANAVANS' + CASE
		WHEN P.ALINANAVANS > 0 THEN ''
		ELSE '2'
	END KASAKONT,
	CONVERT(BIT,
	1) TF,
	CONVERT(BIT,
	0)SEVKSIZ,
	CASE
		WHEN P.ALINANAVANS > 0 THEN 0
		ELSE P.ALINANAVANS * -1
	END AVANS,
	PO.POLIKLINIKADI,
	P.PLANTURU,
	CASE
		WHEN P.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ')
		AND P.SIGORTALITURU IN('H', 'K', 'D') THEN 'ÇALIŞAN'
		WHEN P.GUVENCE2 IN ('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ')
		AND(ISNULL(P.SIGORTALITURU,
		'E') = 'E'
			OR P.SIGORTALITURU = '') THEN 'EMEKLİ'
		ELSE ''
	END EMEKLI
FROM
	V_PROTOKOL P WITH(NOLOCK)
INNER JOIN POLIKLINIK PO WITH(NOLOCK) ON
	P.PoliklinikID = PO.PoliklinikID
INNER JOIN KURUMLAR K WITH(NOLOCK) ON K.KurumID = P.KurumID
WHERE
	((P.ALINANAVANS > 0)
		Or(P.ALINANAVANS2 > 0))
	AND(P.BARKODKODU = 21290798)
	AND(P.ODEMETAMAM IS NULL
		OR P.ODEMETAMAM = 0)
UNION ALL
SELECT
	0 PRORECID,
	A.GUVENCE2,
	A.TABLEX,
	A.SIRANO,
	convert(bit,
	0) ACIL,
	8 KURAL,
	A.TARIH,
	'' KODU,
	'KATKI PAYI (' + A.POLIKLINIKADI + ')' ACIKLAMA,
	1 ADET,
	SUM(A.TUTAR_ODENECEK) FIYATI,
	SUM(A.TUTAR_ODENECEK)TUTARI,
	KURUMADI,
	0 KASAKONT,
	CONVERT(BIT,
	1) TF,
	CONVERT(BIT,
	0) SEVKSIZ,
	0.00 AVANS,
	A.POLIKLINIKADI,
	A.PLANTURU,
	A.EMEKLI
FROM
	(
	SELECT
		0 PRORECID,
		P.GUVENCE2,
		'PROTOKOL40' TABLEX,
		P.BARKODKODU,
		P.TARIH,
		P.SIRANO,
		P.POLIKLINIKADI,
		K.KURUMADI,
		CASE
			WHEN P.GUVENCE2 IN('SSK','BAĞKUR','EMEKLİ SANDIĞI','RESMİ') AND P.SIGORTALITURU IN('H', 'K', 'D') THEN 'ÇALIŞAN'
				WHEN P.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ')
					AND(ISNULL(P.SIGORTALITURU,
					'E') = 'E'
						OR P.SIGORTALITURU = '') THEN 'EMEKLİ'
					ELSE '' END EMEKLI,
					CAST((P40.FIYATI * (CASE
						WHEN(P.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', '2022', '1005', 'VATANSIZ', 'YABANCI', 'YABANCI İSTEK', '60 - G', '18 YAŞ ALTI', 'YABANCI ÖĞRENCİ', 'KAPSAM DIŞI', 'STAJYER AVUKAT', 'AİHM', 'ACİL HAL', 'TBMM', 'S.G.K.'))
							AND (P.SIGORTALITURU = 'E')
								AND(U.KATPAY_SGKE > 0) THEN U.KATPAY_SGKE
								WHEN(P.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', '2022', '1005', 'VATANSIZ', 'YABANCI', 'YABANCI İSTEK', '60-G', '18 YAŞ ALTI', 'YABANCI ÖĞRENCİ', 'KAPSAM DIŞI', 'STAJYER AVUKAT', 'AİHM', 'ACİL HAL', 'TBMM', 'S.G.K.'))
									AND(P.SIGORTALITURU IS NULL
										OR P.SIGORTALITURU IN('H', 'K', 'D', ''))
										AND(U.KATPAY_SGKC > 0) THEN U.KATPAY_SGKC
										WHEN(P.GUVENCE2 = '4447')
											AND(P.TARIH >= '2010-04-01') THEN 20
											WHEN(P.GUVENCE2 = '2828')
												AND(P.TARIH >= '2010-04-01') THEN 10
												WHEN(P.GUVENCE2 = 'YEŞİLKART')
													AND(P.TARIH >= '2010-04-01')
														AND(U.KATPAY_YESILKART > 0) THEN U.KATPAY_YESILKART
														WHEN (P.GUVENCE2 = 'YEŞİLKART')
															AND(P.TARIH < '2010-04-01')
																AND(U.KATPAY_SGKE > 0) THEN U.KATPAY_SGKE
																WHEN (P.GUVENCE2 = 'RESMİ')
																	AND(U.KATPAY_RESMI > 0) THEN U.KATPAY_RESMI
																	ELSE 0
																END) * 0.01 * P40.ADET_CARP) AS DECIMAL (18,
					2)) AS TUTAR_ODENECEK,
					P.PLANTURU
				FROM
					V_PROTOKOLY P WITH(NOLOCK),
					PROTOKOL40 P40 WITH(NOLOCK),
					UCRET1 U WITH(NOLOCK),
					KURUMLAR K WITH(NOLOCK)
				WHERE
					(P.BARKODKODU = 21290798)
						AND(P40.PROTOKOL = P.SIRANO)
							AND(P.KurumID = K.KurumID)
								AND(P40.KODU = U.KODU)
									AND(P40.KASAKONT = 0)
										AND(P40.FIYATI > 0)
											AND(((P.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', '2022', '1005', 'VATANSIZ', 'YABANCI', 'YABANCI İSTEK', '60-G', '18 YAŞ ALTI', 'YABANCI ÖĞRENCİ', 'KAPSAM DIŞI', 'STAJYER AVUKAT', 'AİHM', 'ACİL HAL', 'TBMM', 'S.G.K.'))
												AND(P.SIGORTALITURU = 'E')
													AND(U.KATPAY_SGKE > 0))
												OR((P.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', '2022', '1005', 'VATANSIZ', 'YABANCI', 'YABANCI İSTEK', '60-G', '18 YAŞ ALTI', 'YABANCI ÖĞRENCİ', 'KAPSAM DIŞI', 'STAJYER AVUKAT', 'AİHM', 'ACİL HAL', 'TBMM', 'S.G.K.'))
													AND(P.SIGORTALITURU IS NULL
														OR P.SIGORTALITURU IN('H', 'K', 'D', ''))
														AND(U.KATPAY_SGKC > 0))
													OR((P.GUVENCE2 = '4447')
														AND(P.TARIH >= '2010-04-01'))
														OR((P.GUVENCE2 = '2828')
															AND(P.TARIH >= '2010-04-01'))
															OR((P.GUVENCE2 = 'YEŞİLKART')
																AND(P.TARIH >= '2010-04-01')
																	AND(U.KATPAY_YESILKART > 0))
																OR((P.GUVENCE2 = 'YEŞİLKART')
																	AND(P.TARIH < '2010-04-01')
																		AND(U.KATPAY_SGKE > 0))
																	OR((P.GUVENCE2 = 'RESMİ')
																		AND(U.KATPAY_RESMI > 0)))
									UNION ALL
										SELECT
											0 PRORECID,
											P.GUVENCE2,
											'PROTOKOL20' TABLEX,
											P.BARKODKODU,
											P.TARIH,
											P.SIRANO,
											P.POLIKLINIKADI,
											K.KURUMADI,
											CASE
												WHEN P.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ')
													AND P.SIGORTALITURU IN('H', 'K', 'D') THEN 'ÇALIŞAN'
													WHEN P.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ')
														AND(ISNULL(P.SIGORTALITURU,
														'E') = 'E'
															OR P.SIGORTALITURU = '') THEN 'EMEKLİ'
														ELSE ''
													END EMEKLI,
													CAST((P20.FIYATI * (CASE
														WHEN(P.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', '2022', '1005', 'VATANSIZ', 'YABANCI', 'YABANCI İSTEK', '60-G', '18 YAŞ ALTI', 'YABANCI ÖĞRENCİ', 'KAPSAM DIŞI', 'STAJYER AVUKAT', 'AİHM', 'ACİL HAL', 'TBMM', 'S.G.K.'))
															AND(P.SIGORTALITURU = 'E')
																AND(U.KATPAY_SGKE > 0) THEN U.KATPAY_SGKE
																WHEN(P.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', '2022', '1005', 'VATANSIZ', 'YABANCI', 'YABANCI İSTEK', '60-G', '18 YAŞ ALTI', 'YABANCI ÖĞRENCİ', 'KAPSAM DIŞI', 'STAJYER AVUKAT', 'AİHM', 'ACİL HAL', 'TBMM', 'S.G.K.'))
																	AND(P.SIGORTALITURU IS NULL
																		OR P.SIGORTALITURU IN('H', 'K', 'D', ''))
																		AND(U.KATPAY_SGKC > 0) THEN U.KATPAY_SGKC
																		WHEN(P.GUVENCE2 = '4447')
																			AND(P.TARIH >= '2010-04-01') THEN 20
																			WHEN(P.GUVENCE2 = '2828')
																				AND(P.TARIH >= '2010-04-01') THEN 10
																				WHEN(P.GUVENCE2 = 'YEŞİLKART')
																					AND(P.TARIH >= '2010-04-01')
																						AND(U.KATPAY_YESILKART > 0) THEN U.KATPAY_YESILKART
																						WHEN(P.GUVENCE2 = 'YEŞİLKART')
																							AND(P.TARIH < '2010-04-01')
																								AND(U.KATPAY_SGKE > 0) THEN U.KATPAY_SGKE
																								WHEN(P.GUVENCE2 = 'RESMİ')
																									AND(U.KATPAY_RESMI > 0) THEN U.KATPAY_RESMI
																									ELSE 0
																								END) * 0.01 * P20.ADET_CARP) AS DECIMAL(18,
													2)) AS TUTAR_ODENECEK,
													P.PLANTURU
												FROM
													V_PROTOKOL P WITH(NOLOCK),
													PROTOKOL20 P20 WITH(NOLOCK),
													UCRET1 U WITH(NOLOCK),
													KURUMLAR K WITH(NOLOCK)
												WHERE
													(P.BARKODKODU = 21290798)
														AND(P20.PROTOKOL = P.SIRANO)
															AND(P20.KODU = U.KODU)
																AND(P20.KASAKONT = 0)
																	AND(P20.FIYATI > 0)
																		AND(((P.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', '2022', '1005', 'VATANSIZ', 'YABANCI', 'YABANCI İSTEK', '60-G', '18 YAŞ ALTI', 'YABANCI ÖĞRENCİ', 'KAPSAM DIŞI', 'STAJYER AVUKAT', 'AİHM', 'ACİL HAL', 'TBMM', 'S.G.K.'))
																			AND (P.SIGORTALITURU = 'E')
																				AND(U.KATPAY_SGKE > 0))
																			OR((P.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', '2022', '1005', 'VATANSIZ', 'YABANCI', 'YABANCI İSTEK', '60 - G', '18 YAŞ ALTI', 'YABANCI ÖĞRENCİ', 'KAPSAM DIŞI', 'STAJYER AVUKAT', 'AİHM', 'ACİL HAL', 'TBMM', 'S.G.K.'))
																				AND (P.SIGORTALITURU IS NULL
																					OR P.SIGORTALITURU IN('H', 'K', 'D', ''))
																					AND(U.KATPAY_SGKC > 0))
																				OR((P.GUVENCE2 = '4447')
																					AND(P.TARIH >= '2010-04-01'))
																					OR((P.GUVENCE2 = '2828')
																						AND(P.TARIH >= '2010-04-01'))
																						OR((P.GUVENCE2 = 'YEŞİLKART')
																							AND(P.TARIH >= '2010-04-01')
																								AND(U.KATPAY_YESILKART > 0))
																							OR((P.GUVENCE2 = 'YEŞİLKART')
																								AND(P.TARIH < '2010-04-01')
																									AND(U.KATPAY_SGKE > 0))
																								OR((P.GUVENCE2 = 'RESMİ')
																									AND(U.KATPAY_RESMI > 0)))) A
GROUP BY
	A.PLANTURU,
	A.EMEKLI,
	A.GUVENCE2,
	A.TABLEX,
	A.BARKODKODU,
	A.TARIH,
	A.SIRANO,
	A.POLIKLINIKADI,
	KURUMADI
UNION ALL
SELECT
	0 PRORECID,
	A.GUVENCE2,
	A.TABLEX,
	A.SIRANO,
	convert(bit,
	0) ACIL,
	9 KURAL,
	A.TARIH,
	'' KODU,
	'Yurtdışı SSK Muayene Katılım Payı (' + A.POLIKLINIKADI + ')' ACIKLAMA,
	1 ADET,
	SUM(A.TUTAR_ODENECEK) FIYATI,
	SUM(A.TUTAR_ODENECEK) TUTARI,
	0 KASAKONT,
	CONVERT(BIT,
	1) TF,
	CONVERT(BIT,
	0) SEVKSIZ,
	KURUMADI,
	0.00 AVANS,
	A.POLIKLINIKADI,
	A.PLANTURU,
	CASE
		WHEN A.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ')
		AND A.SIGORTALITURU IN('H', 'K', 'D') THEN 'ÇALIŞAN'
		WHEN A.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ')
		AND(ISNULL(A.SIGORTALITURU,
		'E') = 'E'
			OR A.SIGORTALITURU = '') THEN 'EMEKLİ'
		ELSE ''
	END EMEKLI
FROM
	(
	SELECT
		0 PRORECID,
		P.PLANTURU,
		P.SIGORTALITURU,
		P.GUVENCE2,
		'PROTOKOL' AS TABLEX,
		P.BARKODKODU,
		P.TARIH,
		P.SIRANO,
		P.POLIKLINIKADI,
		K.KURUMADI,
		(
		SELECT
			TOP 1 VEZNE_YURTDISIHASTA_KATILIM_PAYI
		FROM
			SETUP WITH(NOLOCK)) TUTAR_ODENECEK
	FROM
		V_PROTOKOL P WITH(NOLOCK),
		PROTOKOL20 P20 WITH(NOLOCK),
		KURUMLAR K WITH(NOLOCK)
	WHERE
		(P.BARKODKODU = 21290798)
			AND(P.PROVIZYONNO = 'Yurt dışı ssk')
				AND(P20.PROTOKOL = P.SIRANO)
					AND(P20.KASAKONT = 0)
						AND(P20.FIYATI > 0)
							AND(P20.KODU IN('520030', '401010'))/*normal, acil, diş muayeneleri*/
								AND(P.KURUMLAR_KURUMADI NOT LIKE '%ALMANYA%')
									AND((SELECT COUNT(*)
								FROM
									KURUMLAR K WITH(NOLOCK)
								WHERE
									K.KurumID = P.KurumID
									AND K.YURTDISI_ULKE = 'ALMANYA') = 0) ) A
GROUP BY
	A.PLANTURU,
	A.SIGORTALITURU,
	A.GUVENCE2,
	A.TABLEX,
	A.BARKODKODU,
	A.TARIH,
	A.SIRANO,
	A.POLIKLINIKADI,
	KURUMADI UNION ALL
SELECT
	0 PRORECID,
	A.GUVENCE2,
	'PAKETLERA' TABLEX,
	A.SIRANO,
	convert(bit,
	0) ACIL,
	61 KURAL,
	A.TARIH,
	KURUMADI,
	'' KODU,
	'Poliklinik Paket İşlem Ücretleri (' + A.POLIKLINIKADI + ')' ACIKLAMA,
	1 ADET,
	SUM(A.TUTAR_ODENECEK) FIYATI,
	SUM(A.TUTAR_ODENECEK) TUTARI,
	0 KASAKONT,
	CONVERT(BIT,
	1) TF,
	CONVERT(BIT,
	0) SEVKSIZ,
	0.00 AVANS,
	A.POLIKLINIKADI,
	A.PLANTURU,
	CASE
		WHEN A.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ')
		AND A.SIGORTALITURU IN('H', 'K', 'D') THEN 'ÇALIŞAN'
		WHEN A.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ')
		AND(ISNULL(A.SIGORTALITURU,
		'E') = 'E'
			OR A.SIGORTALITURU = '') THEN 'EMEKLİ'
		ELSE ''
	END EMEKLI
FROM
	(
	SELECT
		0 PRORECID,
		P.PLANTURU,
		P.SIGORTALITURU,
		P.GUVENCE2,
		P.BARKODKODU,
		P.TARIH,
		P.SIRANO,
		P.POLIKLINIKADI,
		PKT.TUTAR TUTAR_ODENECEK,
		K.KURUMADI
	FROM
		V_PROTOKOL P WITH(NOLOCK),
		PAKETLER PKT WITH(NOLOCK),
		KURUMLAR K WITH(NOLOCK)
	WHERE
		(P.BARKODKODU = 21290798)
			AND(P.GUVENCE2 = 'ÜCRETLİ SUT')
				AND(PKT.PROTOKOL = P.SIRANO)
					AND(PKT.YATTUR = 0)
						AND(PKT.KASAKONT = 0)
							AND(PKT.TUTAR > 0)) A
GROUP BY
	A.PLANTURU,
	A.SIGORTALITURU,
	A.GUVENCE2,
	A.BARKODKODU,
	A.TARIH,
	A.SIRANO,
	A.POLIKLINIKADI,
	KURUMADI UNION ALL
SELECT
	0 PRORECID,
	A.GUVENCE2,
	'PAKETLERY' TABLEX,
	A.SIRANO,
	convert(bit,
	0) ACIL,
	62 KURAL,
	A.TARIH,
	KURUMADI,
	'' KODU,
	'Klinik Paket İşlem Ücretleri (' + A.POLIKLINIKADI + ')' ACIKLAMA,
	1 ADET,
	SUM(A.TUTAR_ODENECEK) FIYATI,
	SUM(A.TUTAR_ODENECEK) TUTARI,
	0 KASAKONT,
	CONVERT(BIT,
	1) TF,
	CONVERT(BIT,
	0) SEVKSIZ,
	0.00 AVANS,
	A.POLIKLINIKADI,
	A.PLANTURU,
	CASE
		WHEN A.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ') AND A.SIGORTALITURU IN('H', 'K', 'D') THEN 'ÇALIŞAN'
		WHEN A.GUVENCE2 IN('SSK', 'BAĞKUR', 'EMEKLİ SANDIĞI', 'RESMİ')
		AND(ISNULL(A.SIGORTALITURU,
		'E') = 'E'
			OR A.SIGORTALITURU = '') THEN 'EMEKLİ'
		ELSE ''
	END EMEKLI
FROM
	(
	SELECT
		0 PRORECID,
		P.PLANTURU,
		P.SIGORTALITURU,
		P.GUVENCE2,
		P.BARKODKODU,
		P.TARIH,
		P.SIRANO,
		P.POLIKLINIKADI,
		K.KURUMADI,
		PKT.TUTAR AS TUTAR_ODENECEK
	FROM
		V_PROTOKOLY P WITH(NOLOCK),
		PAKETLER PKT WITH(NOLOCK),
		KURUMLAR K WITH(NOLOCK)
	WHERE
		(P.BARKODKODU = 21290798)
			AND(P.GUVENCE2 = 'ÜCRETLİ SUT')
				AND(PKT.PROTOKOL = P.SIRANO)
					AND(PKT.YATTUR = 0)
						AND(PKT.KASAKONT = 0)
							AND(PKT.TUTAR > 0)) A
GROUP BY
	A.PLANTURU,
	A.SIGORTALITURU,
	A.GUVENCE2,
	A.BARKODKODU,
	A.TARIH,
	A.SIRANO,
	A.POLIKLINIKADI,
	KURUMADI